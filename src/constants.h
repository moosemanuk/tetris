#pragma once
#include <raylib.h>

namespace constants
{
    const float SCREEN_HEIGHT = 630;
    const float SCREEN_WIDTH = 500;

    const int GRID_ROWS = 20;
    const int GRID_COLS = 10;
    const int GRID_CELL_SIZE = 30;

}